import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ATM = () => {
  return (
    <View>
      <Text>index</Text>
    </View>
  );
};

export default ATM;

const styles = StyleSheet.create({});
